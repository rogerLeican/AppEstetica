<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.appestetica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_women_nav_cartoon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_women_nav_cartoon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppEstetica">
        <activity
            android:name=".ui.clients.ClientFormActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppEstetica.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>